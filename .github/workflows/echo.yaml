name: Run echo

on: 
    workflow_call: 

        
jobs:
  reusable_workflow_job:
    runs-on: ubuntu-latest
    steps:
        - name: Checkout repository
          uses: actions/checkout@v4
        
        - name: Checkout repository of reusable action
          uses: actions/checkout@v4
          with:
            repository: sparkmuse/sharedworkflows@9b4c2019ce8200b02b23e06e527e9d90fed580bb
            path: ./sharedactions
        
        - run: ls ./sharedactions/.github/actions -a

        - name: Use it
          uses: ./sharedactions/.github/actions/echo-hello-world
